tup=(2,1,3,1)
# tup.index(which elements index you want to find)
# print(tup.index(2)) #returns the index of the first occurrence of the element in the tuple

print(tup.count(1)) #returns the number of occurrences of the element in the tuple
